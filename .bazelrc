# build options
build --color=yes
build --cxxopt='-std=c++17'

# build targets
build:all kvstore:kvstore_server

# test options
test --test_output=all

# test targets
test:all tests:kvstore_test

# clean options
clean --async
